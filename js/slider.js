$(document).ready(function(){
  $('.slider').slick({
    dots: true,
    slidesToShow: 3,
    speed: 1000,
    autoplay: true,
    autoplaySpeed: 2000,
    variableWidth: true,
    centerMode: true,
  });
});
